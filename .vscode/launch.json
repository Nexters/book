{
  "configurations": [
    {
      "name": "앱 실행",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "args": ["--port", "8080"],
      "program": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
